#include <math.h>
#include <stdio.h>

int main() {
   double number, cubeRoot;

   printf("Enter a number: ");
   scanf("%lf", &number);

   
   squareRoot = cbrtf(number);

   printf("cube root of %.2lf =  %.2lf", number, cubeRoot);

   return 0;
}
